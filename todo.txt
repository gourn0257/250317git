=== 2025.03.25 ===
World.id는 몇 자로 받는지
World.title은 글자 제한 어디까지 할지
정해야 한다

World.id 를 유튜브 링크처럼 무작위로 할지, Auto Incremental로 할지

switch 배열 안에 아무 요소도 없으면 워크스페이스에 회색 뜨기

미리 한 변수에다가 이미지들이 저장되는 파일 주소 "C:/sherLOCK/image/"(예시)를 담는다
그리고 그 변수에 World.imgURL 변수를 더하게 하면 된다

스테이지가 연결되어 있으면 스테이지끼리 이동할 수 있다는 뜻이다
하지만 Stage.gateOpen 변수가 false로 되어 있는 스테이지는 들어갈 수 없다

Item.detailedImgURL은 인벤토리에서 아이템을 눌렀을 때 뜨는 이미지다
인벤토리에서 보는 아이템과 자세히 보기에서의 아이템 이미지가 다르다

스테이지와 스테이지가 연결 되어 있고
컷과 컷이 연결되어 있다

World.role 안에 있는 키값에 user_name 대신 user_id를 저장. 유저 이름이 바뀔 수 있음

직접 데이터에 스테이지 수를 넣지 말고 스테이지.length 라던가 반복문 써서 출력하게

connectStage, connectCut 메소드 인자 값을 객체로 받는 이유는 컷 위치가 "stage[i].cut[i]"이기 때문

Stage.timeLimit, Cut.timeLimit에 값이 아무것도 없으면 false로 생각하면 된다

스테이지 개수, 컷 개수 제한 몇개로?

업적은 나중에 만들기로

분기와 멀티엔딩을 구현하려면 변수는 필요하다

class World type을 두는 것보다
class World를 두 개로 하는 게 낫다
예를 들어서
class World_a
class World_b
이렇게 하는 게 낫다
